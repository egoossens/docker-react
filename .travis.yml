language: generic
sudo: required
services:
    - docker
before_install:
    - docker build -t egoossens/docker-react -f Dockerfile.dev .
script:
    - docker run -e CI=true egoossens/docker-react npm run test